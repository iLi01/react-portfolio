import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


// run the App, don't need to play with it
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);
